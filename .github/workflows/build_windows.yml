name: Build For Windows

on:
  workflow_call:

jobs:
  setup_metadata:
    uses: ./.github/workflows/setup_metadata.yml

  build:
    needs: setup_metadata
    runs-on: ${{ matrix.os }}

    strategy:
      matrix:
        target: [windows]
        arch: [x86_64]
        build_type: [release, debug]
        include:
          - build_type: release
            cmake_build_type_keyword: Release
          - build_type: debug
            cmake_build_type_keyword: RelWithDebInfo
          - os: windows-latest
            target: windows
          - target: windows
            arch: x86_64
            cmake_arch_keyword: x64

    steps:
      - name: Check out code
        uses: actions/checkout@v4

      - name: Set up MSVC Dev Cmd
        uses: ilammy/msvc-dev-cmd@7315a94840631165970262a99c72cfb48a65d25d # v1.12.0
        with:
          arch: ${{ matrix.cmake_arch_keyword }}

      - name: Configure
        shell: cmd
        run: |
          cmake -S "${{ github.workspace }}" -B "${{ github.workspace }}\build" -G Ninja ^
            -DCMAKE_BUILD_TYPE="${{ matrix.cmake_build_type_keyword }}" ^
            -DVB_WARNINGS_AS_ERRORS=ON ^
            -DVB_HEADLESS=OFF

      - name: Build
        shell: cmd
        run: cmake --build "${{ github.workspace }}\build"

      - name: Test
        shell: cmd
        working-directory: build
        run: ctest --output-on-failure

      - name: Stage binaries
        shell: bash
        run: |
          dist="${{ github.workspace }}/dist"
          mkdir -p "$dist"
          cp "${{ github.workspace }}/build/voxel_browser.exe" "$dist/"
          cp "${{ github.workspace }}/build/voxel_browser_server.exe" "$dist/"

      - name: Export binaries
        uses: actions/upload-artifact@v4
        with:
          name: ${{ needs.setup_metadata.outputs.project_name }}-${{ matrix.target }}-${{ matrix.arch }}-${{ matrix.build_type }}
          path: ${{ github.workspace }}/dist/*
          if-no-files-found: error
